language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t fazlizekiqi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true fazlizekiqi/docker-react npm run test
